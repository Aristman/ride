Необходимо пересмотреть концепцию и подход для оркестрации чата
Основные требования
1. Запрос пользователся обрабатывается анализатором запроса пользователя. По этому делается набросок плана решения задачи. На этом этапе происходит оценка неопределенности задачи - если задача требует уточнения, агент должен уточнить у пользователя нужную информацию. После всех уточнений сам запрос с уточняющими ответами собирается для генерации плана решения. Вход анализатора - запрос пользователя и список агентов-тулзов, доступных для разработки. Выход - сгенерированный план и сборный запрос+уточнения (далее это называем "Промпт запроса").
2. В чате на все время работы отображается сообщение с текущим выполняемым шагом. Вид сообщения "Выполняю постороение плана..." Перед сообщением нужно показать анимированную иконку выполнения работы. Данное сообщение будет пропадать после завершения шага. На следующем шаге будет свое новое информационное сообщение.
3. Далее План отображается в отдельном блоке над блоком запроса пользователя (этот блок в виде двух строк: План Работ: - 1 строка, Выполняемый шаг - 2 строка. По клику этот блок разворачивается вверх с полным списком плана. По второму клику - сворачивается опять с 2 строки)
4. Агент - оркестратор выполняет шаги плана. В чате отображаются результаты каждого шага (если агент-тулз имеет выходные дынные для отображения).
5. Оркестартор хранит исходный план с отметками выполнения (обновляет его в ui по мере выполнения), Промпт запрос, результаты работы каждого агента (сырые данные + данные для отображения). Нужные данные для работы агентов по плану берутся из хранилища. Результаты работы агентов-тулзов сохраняются в хранилище.
6. По мере реализации плана возможны изменения самой структуры плана. Для этого используется отдельный агент или существующий агент. План при этом обновляется и в памяти и в ui.
7. Должна быть возможноть уточнения у пользователя дополнительной информации по мере выполнения плана.
8. После завершения всех стадий плана оркестратор должен собрать всю информацию по решению и отобразить в чате отдельным сообщением. Для этого возможно использование отдельного агента-тулза для генерации финального сообщения.
9. Все операции внутри оркестратора нужно делать через отдельных агентов-тулзов. Агент может быть как атомарным так и содержащим фиксиНеобходимо пересмотреть концепцию и подход для оркестрации чата
Основные требования
1. Запрос пользователся обрабатывается анализатором запроса пользователя. По этому делается набросок плана решения задачи. На этом этапе происходит оценка неопределенности задачи - если задача требует уточнения, агент должен уточнить у пользователя нужную информацию. После всех уточнений сам запрос с уточняющими ответами собирается для генерации плана решения. Вход анализатора - запрос пользователя и список агентов-тулзов, доступных для разработки. Выход - сгенерированный план и сборный запрос+уточнения (далее это называем "Промпт запроса").
2. В чате на все время работы отображается сообщение с текущим выполняемым шагом. Вид сообщения "Выполняю постороение плана..." Перед сообщением нужно показать анимированную иконку выполнения работы. Данное сообщение будет пропадать после завершения шага. На следующем шаге будет свое новое информационное сообщение.
3. Далее План отображается в отдельном блоке над блоком запроса пользователя (этот блок в виде двух строк: План Работ: - 1 строка, Выполняемый шаг - 2 строка. По клику этот блок разворачивается вверх с полным списком плана. По второму клику - сворачивается опять с 2 строки)
4. Агент - оркестратор выполняет шаги плана. В чате отображаются результаты каждого шага (если агент-тулз имеет выходные дынные для отображения).
5. Оркестартор хранит исходный план с отметками выполнения (обновляет его в ui по мере выполнения), Промпт запрос, результаты работы каждого агента (сырые данные + данные для отображения). Нужные данные для работы агентов по плану берутся из хранилища. Результаты работы агентов-тулзов сохраняются в хранилище.
6. По мере реализации плана возможны изменения самой структуры плана. Для этого используется отдельный агент или существующий агент. План при этом обновляется и в памяти и в ui.
7. Должна быть возможноть уточнения у пользователя дополнительной информации по мере выполнения плана.
8. После завершения всех стадий плана оркестратор должен собрать всю информацию по решению и отобразить в чате отдельным сообщением. Для этого возможно использование отдельного агента-тулза для генерации финального сообщения.
9. Все операции внутри оркестратора нужно делать через отдельных агентов-тулзов. Агент может быть как атомарным так и содержащим фиксированную цепочку выполнения (сборным из атомарных агентов-тулзов)рованную цепочку выполнения (сборным из атомарных агентов-тулзов)

---

Вот финализированный **промпт для разработки оркестрации чата** в рамках плагина для IDE JetBrains — в чистом виде, без кода, но со структурой, понятной разработчикам и архитекторам.

## 💡 **Промпт: Разработка оркестратора чата с агентами-тулзами для IDE-плагина**

### 🎯 Цель

Создать систему оркестрации чата, обеспечивающую интеллектуальную обработку пользовательских запросов через сеть агентов-тулзов, с визуализацией этапов выполнения, управлением состоянием и интерактивным взаимодействием с пользователем внутри IDE JetBrains.

### 🧩 **1. Анализатор запроса пользователя**

Задача анализатора — интерпретировать запрос пользователя и сформировать основу для выполнения.

**Основные функции:**

* Принимает на вход исходный запрос пользователя и список доступных агентов-тулзов.
* Анализирует запрос, формирует черновой план решения задачи.
* Оценивает уровень неопределенности запроса.
* Если задача требует уточнений — задает пользователю дополнительные вопросы.
* После получения ответов объединяет исходный запрос и уточнения в единый документ — *Промпт запроса*.
* Генерирует итоговый план работ на основе уточненных данных.

**Результат этапа:**

* Структурированный *План работ* (пошаговый список действий с назначением агентов).
* *Промпт запроса* — собранная версия исходного запроса с уточнениями.

### 💬 **2. Отображение текущего статуса работы**

Во время выполнения каждого шага в чате отображается активное информационное сообщение:

* Формат: «🔄 Выполняю построение плана...»
* Перед текстом — анимированная иконка активности.
* Сообщение автоматически исчезает после завершения шага.
* Для каждого этапа отображается своё сообщение с соответствующим описанием.

### 📋 **3. Блок плана в UI**

Над полем ввода пользователя всегда отображается сводный блок плана:

* Первая строка — «План работ: [название задачи]»
* Вторая строка — «Текущий шаг: [описание шага]»
* По клику блок разворачивается вверх, показывая весь план со статусами шагов.
* Повторный клик — сворачивает блок обратно до двух строк.

Блок динамически обновляется при изменении статусов выполнения шагов и при изменении структуры плана.

### ⚙️ **4. Работа оркестратора**

Оркестратор управляет жизненным циклом задачи:

**Основные функции:**

* Запускает выполнение шагов в соответствии с планом.
* На каждом шаге вызывает соответствующий агент-тул.
* Получает и обрабатывает результаты выполнения.
* Отображает промежуточные результаты в чате (если они предназначены для визуализации).
* Обновляет статусы шагов и отображает прогресс в UI.
* Сохраняет все промежуточные результаты, состояние плана и контекст выполнения в хранилище.

### 🗃️ **5. Хранилище состояния**

Хранилище используется для сохранения и доступа к данным между шагами.

**Содержит:**

* Исходный и текущий план с отметками выполнения.
* Промпт запроса (исходный запрос + уточнения).
* Результаты выполнения агентов (сырые и обработанные данные).
* Контекст выполнения (промежуточные решения, ответы пользователя, дополнительные данные).

### 🔄 **6. Динамическое обновление плана**

* В процессе выполнения возможна корректировка структуры плана.
* Для этого используется отдельный агент, ответственный за изменение плана.
* При изменении структура плана синхронно обновляется в памяти и в интерфейсе пользователя.
* После изменения оркестратор продолжает выполнение с обновленным контекстом.

### 🙋 **7. Интерактивное уточнение**

* В любой момент выполнения оркестратор может обратиться к пользователю с вопросами, если агенту не хватает данных.
* Ответы пользователя автоматически добавляются в контекст и сохраняются в хранилище.
* После получения данных выполнение продолжается с учетом новой информации.

### ✅ **8. Завершение работы**

* После выполнения всех этапов оркестратор собирает результаты всех агентов.
* Формируется итоговое сообщение, содержащее:

    * краткий обзор процесса решения;
    * итоговые результаты;
    * при необходимости — ссылки или артефакты, созданные агентами.
* Финальное сообщение отображается в чате как отдельный блок, обозначающий завершение работы над задачей.

### 🧠 **9. Агентно-тулизированная структура**

* Все операции внутри оркестратора выполняются через агентов-тулзов.
* Агент может быть:

    * **атомарным** — выполняет одну конкретную функцию (например, анализ текста, генерация кода, визуализация);
    * **композитным** — объединяет несколько атомарных агентов в фиксированную цепочку действий.
* Оркестратор управляет вызовами агентов, передает им контекст и собирает результаты в единое решение.

### 🔧 **10. Общие требования**

* Архитектура должна быть модульной: возможность добавления новых агентов без изменения ядра оркестратора.
* Все взаимодействие — через унифицированный интерфейс агентов.
* Состояние выполнения и контекст — сериализуемые и сохраняемые.
* UI должен быть отзывчивым, отображать прогресс и поддерживать интерактивность с пользователем.
* Приоритет — совместимость и удобная интеграция с интерфейсом IDE JetBrains (чат-панель, всплывающие уведомления, интерактивные блоки).

---
