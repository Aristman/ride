# 🧪 Агент автогенерации и запуска Unit-тестов

## 📊 Метрики проекта

- **Статус**: 📋 ЗАПЛАНИРОВАНО
- **Версия**: 1.0.0
- **Дата создания**: 2025-11-05
- **Ответственный**: Команда разработки Ride
- **Приоритет**: Высокий
- **Оценка**: 2–4 недели на MVP (Kotlin/Java), +1–2 недели на Dart

---

## 🎯 Цели фичи

- Обеспечить автоматическую генерацию unit-тестов по публичному контракту функций.
- Дать пользователю быстрый путь: выбрать файл через `@`-пикер → сгенерировать тесты → сохранить → запустить → получить результаты в чате.
- Интегрировать запуск тестов в существующую консоль плагина и красиво отрендерить результаты.

**Пользовательские ценности:**
- Снижение порога входа в тестирование.
- Ускорение разработки (редуцирование boilerplate).
- Повышение надежности через быстрый фидбек-цикл в чате.

---

## 🏗️ Архитектура и подход

- **Парадигма**: Clean Architecture, SOLID, разделение по слоям.
- **Стратегия по языкам**: общий оркестратор + специализированные агенты под язык.
  - `TestingAgentOrchestrator` — принимает файл, делит на тестируемые единицы (публичные функции/классы), вызывает конкретный языковой агент.
  - `KotlinTestingAgent` / `JavaTestingAgent` / `DartTestingAgent` — генерация тестов и размещение в корректной структуре проекта.
- **Чанкинг**: чанк = публичная функция/метод/класс; приватные игнорируются, тестируется публичный контракт.
- **Генерация тестов**: через LLM + статический анализ (парсинг сигнатур, аннотаций, исключений, nullable/nonnull контрактов).
- **Сохранение**: в проектно-специфичную структуру (Gradle/Maven для JVM, `test/` для Dart). Конфигурируемые правила.
- **Запуск**: делегирование имеющейся консольной инфраструктуре плагина для запуска тестов (Gradle/Maven/test runner; Dart `dart test`).
- **Отчетность**: агрегация результатов (passed/failed/skipped), ошибки ассертов, трейс; рендер в чате с фильтрами и сводкой.

---

## 📦 Область покрытия MVP

- Ввод файла через `@`-пикер с фильтрацией по имени (workspace files).
- Поддержка языков: Kotlin, Java. Dart — за флагом/фазой 2.
- Фреймворки по умолчанию:
  - Kotlin/Java: JUnit5 + AssertJ (или стандартные Assertions)
  - Dart: `package:test` (в фазе 2)
- Генерация базовых позитивных кейсов + каркасы негативных/edge.
- Запуск из чата и отображение результатов в виде сводки с возможностью развернуть детали.

---

## 📅 Фазы разработки

### Phase 1: Дизайн и API
- [ ] Определить интерфейсы `TestingAgentOrchestrator`, `LanguageTestingAgent`.
- [ ] Спецификация протокола взаимодействия с чатом и `@`-пикером.
- [ ] Схема данных: `TestUnit`, `GeneratedTest`, `TestRunResult`.
- [ ] Решение по конфигу (расположение тестов, шаблоны именования файлов/классов).

### Phase 2: Генерация тестов (Kotlin/Java)
- [ ] Реализовать парсер публичных контрактов (Kotlin/Java) — сигнатуры, аннотации, исключения.
- [ ] Реализовать `KotlinTestingAgent` с генерацией JUnit5.
- [ ] Реализовать `JavaTestingAgent` с генерацией JUnit5.
- [ ] Шаблоны тест-классов (package, imports, имя, `@Test`).
- [ ] Сохранение файлов в правильные каталоги (`src/test/kotlin|java`).

### Phase 3: Интеграция запуска и результаты
- [ ] Интеграция с существующим запуском тестов в консоли плагина (Gradle/Maven).
- [ ] Нормализация вывода: парсинг `--tests`/build output в `TestRunResult`.
- [ ] Рендер в чате: сводка (N passed, M failed, duration), список ошибок с трейсами.

### Phase 4: UI/UX и `@`-пикер
- [ ] UI окно выбора файла по `@` с инкрементальным фильтром.
- [ ] Подсветка поддерживаемых типов (Kotlin, Java, Dart).
- [ ] Быстрые действия: «Сгенерировать тесты», «Запустить тесты», «Открыть файл теста».

### Phase 5: Dart поддержка
- [ ] `DartTestingAgent` (анализ публичных функций/классов).
- [ ] Генерация `package:test` кейсов и сохранение в `test/`.
- [ ] Интеграция запуска `dart test` и парсинг результатов.

### Phase 6: Качество и документация
- [ ] Встроенные self-tests для генераторов (snapshot/fixture tests).
- [ ] Документация: гайд по структуре проектов и настройкам.
- [ ] Метрички: покрытие по числу сгенерированных тестов, время отклика, % успешных запусков.

---

## ✅ Критерии приемки (MVP)

- [ ] Пользователь выбирает файл через `@`-пикер, поддерживаются Kotlin/Java.
- [ ] Агент выделяет ≥1 публичную сущность и генерирует тест-класс в корректном месте.
- [ ] Тесты компилируются и запускаются через плагин без ручных правок.
- [ ] Результаты тестов отображаются в чате в удобочитаемом виде (сводка + детали ошибок).
- [ ] Конфигурация путей и шаблонов именования настраивается.

---

## 🔧 Технические детали реализации

- Интерфейсы:
  - `TestingAgentOrchestrator.generateAndRun(filePath): TestRunResult`
  - `LanguageTestingAgent.supports(filePath): Boolean`
  - `LanguageTestingAgent.generate(fileContent): List<GeneratedTest>`
  - `TestPersister.persist(tests): List<Path>`
  - `TestRunner.run(scope): TestRunResult`
- Детект языка: по расширению и/или PSI/AST (если доступно). Минимум — по расширению.
- Именование тестов: `<ClassName>Test` / `<FunctionName>Test` (конфигурируемо).
- Imports: автодобавление JUnit5/AssertJ. Для Kotlin — возможность `kotlin.test`.
- Edge-cases: data classes, suspending functions, generics, exceptions, nullable.

---

## 🧩 Интеграции

- Запуск JVM-тестов: Gradle (`test` task) / Maven (`test` goal). Фильтрация: по классу/пакету при необходимости.
- Запуск Dart-тестов: `dart test` (Phase 5).
- UI: общие экшены в чате; deeplink «Открыть файл теста».

---

## 🔐 Ограничения и риски

- Неполные спеки методов → потребуются ассумпшены и шаблоны входных данных.
- Флейки при зависимостях от времени/IO/сети — рекомендации на моки/стабы.
- Структуры проектов сильно различаются; нужны пресеты + автодетект Gradle/Maven.

---

## 📈 Результаты и метрики на релиз

- Функциональность: генерация и запуск тестов из чата для Kotlin/Java.
- Техметрики: время генерации < 5 с на файл 300–500 LOC; успешный запуск > 95%.
- API: стабильные интерфейсы агентов и раннера.

---

## 🔄 Следующие шаги

- Расширенная генерация данных (property-based, мутирующее тестирование).
- Интеграция с покрытиями (JaCoCo) и целями покрытия.
- Автофиксы простых падений (рекомендации по stub/mock).
