# 🐛 Диагностика проблем с Ollama в Ride плагине

## Проблема: Ответ от Ollama пустой

Если вы видите пустые ответы от нейросети, следуйте этой инструкции:

### 1. Проверьте статус модели

```bash
ollama list
```

Убедитесь что модель установлена:
```
qwen:7b                    2091ee8c8d8f    4.5 GB    X minutes ago
```

### 2. Проверьте статус сервера

```bash
curl -s http://localhost:11434/api/tags | jq '.'
```

Ответ должен быть примерно таким:
```json
{
  "models": [
    {
      "name": "qwen:7b",
      "model": "qwen:7b",
      "modified_at": "2024-11-06T19:30:00.123456+00:00",
      "size": 4567890123,
      "digest": "sha256:..."
    },
    {
      "name": "nomic-embed-text:latest",
      "model": "nomic-embed-text:latest",
      ...
    }
  ]
}
```

### 3. Протестируйте Ollama напрямую

```bash
curl -X POST http://localhost:11434/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen:7b",
    "prompt": "Привет! Как тебя зовут?",
    "stream": false
  }'
```

Ответ должен содержать текст в поле `response`.

### 4. Проверьте настройки в Ride

1. Откройте `Settings → Tools → Ride → LlmConfig`
2. Убедитесь что выбрано `Ollama (Local)`
3. Убедитесь что выбрана модель `Qwen 7B`
4. Проверьте что Base URL: `http://localhost:11434`
5. Нажмите `Apply` и `OK`

### 5. Посмотрите логи

Включите отображение логов: `Help → Show Log in Explorer`

Ищите строки:
```
Ollama API response:
Ollama parsed response text:
```

Это поможет увидеть что возвращает Ollama API и как парсится ответ.

### 6. Попробуйте другие модели

Если Qwen не работает, попробуйте установить другую модель:

```bash
ollama pull llama3:8b
# или
ollama pull mistral:7b
```

И выберите её в настройках плагина.

### 🔧 Распространенные проблемы

#### Проблема: "model not found"
- Убедитесь что модель установлена: `ollama list`
- Проверьте точное имя модели в списке

#### Проблема: Сервер недоступен
- Запустите сервер: `ollama serve`
- Проверьте порт: `curl http://localhost:11434/api/tags`

#### Проблема: Медленные ответы
- Используйте более легкую модель
- Проверьте загрузку CPU и памяти
- Увеличьте timeout в настройках

### 📝 Сообщите о проблеме

Если ничего из этого не помогает, предоставьте:
1. Логи из IDE (Help → Show Log in Explorer)
2. Результаты выполнения команд диагностики выше
3. Снимок экрана настроек плагина

Это поможет выявить точную причину проблемы.