# Исправления A2A оркестратора - Финальная версия

## Проблема
Агент ревью падал с ошибкой "Either 'code' or 'files' must be provided for review" и возникали таймауты при выполнении A2A запросов.

## Корневые причины
1. **Отсутствие передачи данных между агентами** - метод `enrichStepInput` не передавал `generated_code`
2. **A2A режим был отключен** - использовался legacy оркестратор вместо A2A
3. **Агенты не публиковали ответы** - возвращали ответы, но не отправляли их в MessageBus
4. **Нестабильная подписка** - использование `subscribeAll()` работало нестабильно

## Выполненные исправления

### 1. Исправлен метод `enrichStepInput` ✅
**Файл**: `EnhancedAgentOrchestratorA2A.kt`
- Добавлена передача `generated_code` от CODE_GENERATOR к LLM_REVIEW
- Добавлена передача языка программирования (`language`)
- Добавлен fallback для сканирования файлов проекта
- Добавлено подробное логирование

### 2. Включен A2A режим ✅
**Файл**: `EnhancedChatAgent.kt`
- Добавлен вызов `a2aConfig.enableDevelopmentMode()`
- Изменен `executePreparedPlan` для использования A2A оркестратора
- Добавлено логирование инициализации

### 3. Исправлена публикация ответов ✅
**Файлы**: Все A2A агенты
```kotlin
// Было:
return handleRequest(message)

// Стало:
val response = handleRequest(message)
messageBus.publish(response)  // Публикуем в шину!
return response
```

### 4. Исправлена подписка на сообщения ✅
**Файлы**: Все A2A агенты
```kotlin
// Было:
messageBus.subscribeAll { message -> canHandleMessage(message) }

// Стало:
messageBus.subscribe(AgentMessage.Request::class) { request -> canHandleMessage(request) }
```

### 5. Добавлена задержка для подписки ✅
**Файл**: `EnhancedAgentOrchestratorA2A.kt`
```kotlin
// Даем агентам время подписаться на сообщения
kotlinx.coroutines.delay(1000)
```

### 6. Добавлено подробное логирование ✅
- Логирование получения и обработки запросов в агентах
- Логирование отправки A2A запросов в оркестраторе
- Отслеживание передачи данных между агентами

## Результат

### ✅ Исправлено
- Агент ревью теперь получает код от генератора через A2A шину
- Устранены таймауты - агенты отвечают на запросы
- Полноценная работа A2A протокола
- Стабильная передача данных между агентами

### 🔄 Ожидаемый flow
1. **PROJECT_SCANNER** → сканирует проект → возвращает список файлов
2. **CODE_GENERATOR** → генерирует код → возвращает `generated_code`
3. **LLM_REVIEW** → получает код через A2A → проводит ревью

### 🧪 Тестирование
Запустите тот же запрос для проверки:
```
НАпиши класс для создания ряда фибоначчи
```

Теперь план должен выполняться без ошибок и таймаутов.

## Архитектурные улучшения
- Полноценная поддержка A2A протокола
- Надежная передача данных между агентами через MessageBus
- Улучшенное логирование для отладки
- Graceful handling при отсутствии данных
- Стабильная подписка на сообщения

### 9. Исправлены типы A2A запросов ✅
**Файл**: `EnhancedAgentOrchestratorA2A.kt`
- **Проблема**: CODE_GENERATOR получал `TOOL_EXECUTION_REQUEST` вместо `CODE_GENERATION_REQUEST`
- **Решение**: Добавлена обработка `AgentType.CODE_GENERATOR` в `buildA2ARequestForStep`
```kotlin
AgentType.CODE_GENERATOR -> {
    "CODE_GENERATION_REQUEST" to MessagePayload.CustomPayload(
        type = "CODE_GENERATION_REQUEST", 
        data = input
    )
}
```

## Прогресс исправлений
✅ **PROJECT_SCANNER** - работает, отвечает на FILE_DATA_REQUEST  
🔧 **CODE_GENERATOR** - исправлен тип запроса на CODE_GENERATION_REQUEST  
⏳ **LLM_REVIEW** - ожидает получения кода от CODE_GENERATOR